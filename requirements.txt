# Pinned to versions known to work together with PyInstaller on Windows
# NumPy 1.x is used because NumPy 2.x has breaking changes with pandas/matplotlib
numpy==1.26.4
PySide6==6.7.3
matplotlib==3.9.4
pandas==2.2.3
openpyxl==3.1.5
reportlab==4.2.5
pyinstaller==6.10.0
